{
  "name": "wizard",
  "version": "0.1.0",
  "description": "Design intelligence framework for AI coding tools",
  "type": "module",
  "scripts": {
    "build": "bun scripts/build.js",
    "rebuild": "rm -rf dist && bun scripts/build.js"
  }
}
